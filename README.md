## This is videly used Version control.

this is first iteration to the githum repo first downloaded the git
then opend CMD:-  git --version
The reponame is gittutorial

### so step by step process of the git 
* git --version
this will give you the version and we can check if the git is present in the console

* cd "folder path paste"
this will go to the specific folder to push.

* git init 
ititiate the empty the .git in the folder which convert project repo

* git config --global user.name "Ayush Singh"
* git config --global user.email "aayush0761@gmail.com"
this is for the first time we are setting up the account and email identify the git author

* git status
it will give you the thing which need to be for staging

* git add README.md
this step will stage the file to the staging area and next it will commit

* git add .
for adding all files at once

* git status
it will show there is nothing to be added

* cls
open new windown in command prompt

* git commit -m "This is first commit"
this will staged the files in the repo history

* git branch
this wil show you the braches avilable in the repo

* git branch -M main
change the master ---> main branch

* git branch
you can view the new branch

* cls
-----

* git remote add origin "link of the github"
it will connect the remote reo to the local one 

* git status

* git remote -v
gives you origin and the repo link 

* git push origin main
push the reo the github

* git restore README.md
it will restore the 

* git reset [file name]
it will reset the file and unstage that file 

* git diff
it will return you what is changed but not staged (this give before adding)

* git diff --staged
it will give you details of the repo before the changing(this give status after adding)

* git clone "cloning link"
close the repo to the specific folder

* git branch developer
create the new branch 

* git checkout developer
switch the branch to developer or new branch

- > first check out to the main branch then
* git merge developer
merge the main to the development tree

